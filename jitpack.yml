jdk:
  - openjdk11
install:
  - echo "Install"
  - java -version
  - mvn install

#before_install:
#  - yes | sdkmanager --uninstall "ndk-bundle" | sdkmanager --install "ndk;16.1.4479499"

#install:
#  - ./gradlew :library_log:build :library_log:publishToMavenLocal -x :library_log:test
#  - ./gradlew :library_lint:build :library_lint:publishToMavenLocal -x :library_lint:test

#  - find . -name "*.aar"
#  - ./gradlew :moka:build :moka:publishToMavenLocal -x :moka:test